// import type { messages } from '../../../server';
// import type { formula_conversations } from '../../../server';
// import { MessagesTable } from '../../../server';

// // 新建对话，name初始为默认
// const add_conservation = (name: string) => {
//   //   const ;
//   return;
// };

// // 根据话题名字，获取对话消息，传回加载对话
// const load_conservation = (id: string) => {
//   const message = MessagesTable.getManyByConversationId();
// };

// // 实时更新对应对话的消息记录保存/ 仅更改话题名字
// const update_conservation = (id: string) => {};
